import 'package:flutter/cupertino.dart';

class LeaderBorderPage extends StatelessWidget {
  const LeaderBorderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('leader board'),
    );
  }
}
