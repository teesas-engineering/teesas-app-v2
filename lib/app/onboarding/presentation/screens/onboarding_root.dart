import 'package:flutter/material.dart';

class OnboardingRoot extends StatelessWidget {
  const OnboardingRoot({super.key});

  @override
  Widget build(BuildContext context) {
    //include a pageview for onboarding page
    return Scaffold();
  }
}
